{
  "name": "@codegeni/brain",
  "version": "0.1.0",
  "description": "CodeGeni reasoning brain that orchestrates LangGraph and MCP tools",
  "type": "module",
  "main": "dist/server.js",
  "types": "dist/server.d.ts",
  "files": ["dist"],
  "scripts": {
    "build": "tsc -b",
    "clean": "rm -rf dist",
    "dev": "bun run src/server.ts"
  },
  "dependencies": {
    "@codegeni/langgraph": "workspace:*",
    "@codegeni/tools": "workspace:*",
    "zod": "^3.23.8"
  },
  "devDependencies": {
    "@types/node": "^20.12.8"
  }
}
